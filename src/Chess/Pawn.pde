class Pawn {
  int column, row;
  boolean isWhite;
  
  Pawn(int column, int row, boolean isWhite) {
    this.column = column;
    this.row = row;
    this.isWhite = isWhite;
  }
  
  void setColumn() {
  
  }
  
}
